# Open Production Gates

- Only three labeled PLAY transition frames are committed; at least 2,000 locked negative/disabled frames are required.
- No locked OCR ground-truth set exists, so critical exact-field accuracy cannot be measured.
- No locked turn-transition replay exists, so zero false `MY_TURN` cannot be claimed.
- Existing button templates are calibrated for the committed 1280x720 theme only.

Do not change `MOD-PERCEPTION` to `CANDIDATE` or `LOCKED` until these gates have machine-readable evidence.
