# Remaining Decisions Before Lock

- Confirm whether three consecutive pairs may beat a pair of twos in the target game variant.
- Confirm how four consecutive pairs may act out of turn; this belongs to orchestrator policy.
- Add a typed first-game/opening signal before runtime enables the mandatory `3S` gate.
- Add real replay fixtures for pass-cycle reset, round opening and game end transitions.

These are explicit lock blockers, not silently accepted behavior.
